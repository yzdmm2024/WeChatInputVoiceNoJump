# 为什么之前「还是跳转」 + 这次怎么验证

## 根因（frida 实测 + 静态分析确认）
1. **hook 打错了对象。** 旧版只 hook 了 `UIApplication openURL:`。但键盘扩展 `wxkb_plugin.appex`
   是**独立进程**，在扩展里拉起主 app（点语音跳出去）用的是扩展专用 API
   `-[UIInputViewController openURL:]`（不是 UIApplication）。扩展进程里 `UIApplication.sharedApplication`
   本身就是 nil，根本不会走我 hook 的那条路 → 跳转照旧。
2. **很可能只注入了主 app，没注入键盘扩展。** 语音按钮跑在键盘扩展进程里；dylib 只在
   `wxkb.app` 主程序加载，键盘扩展进程里既没有 NSUserDefaults 强制（WcVoiceNoJump=YES），
   也没有拦截逻辑 → 照常跳转。

frida 没法直接验证：键盘扩展 `wxkb_plugin` 和主 app 都带反调试，`frida -U -n wxkb_plugin`
直接 `Failed to attach: timeout was reached`。所以改用**落盘诊断日志**绕过限制。

## 本次修正（out/WxKeyboardNoJump.dylib）
- 跳转拦截从只 hook `UIApplication` 扩展为同时 hook
  `UIInputViewController` / `NSExtensionContext` 的 `openURL:`（覆盖扩展拉起主 app 的全部入口）。
- 保留 `NSUserDefaults` 对 `WBAppSettingsBool_VoiceInput_WcVoiceNoJump` 强制 YES。
- 新增**落盘诊断**：所有加载 / 拦截 / 命中键事件写入 `/var/mobile/wxkbd_diag.log`。

## 部署（关键：两个二进制都要注入）
用 TrollFools / dylib插件库 注入时，**必须同时选**：
- `wxkb.app`（主程序，bundle `com.tencent.wetype`）
- `wxkb.app/PlugIns/wxkb_plugin.appex`（键盘扩展，bundle `com.tencent.wetype.keyboard`）

> 若用 TrollFools 选择 `wxkb.app` 注入，它通常会自动把 dylib 一并注入其扩展；
> 若用其他只注主 app 的工具，需手动把同一个 dylib 也加到 `wxkb_plugin.appex`。

Settings.bundle（外观面板）仍需手动拷进 `wxkb.app/` 根目录，才会在
**系统-设置 → 微信输入法** 下出现「微信键盘免跳转」分组。

## 验证步骤
1. 确认 dylib 已注入上面**两个**二进制，杀掉微信输入法 / 注销使扩展重新加载。
2. 打开微信，切到微信输入法，点语音按钮，观察是否还会跳出去。
3. 用 NewTerm 或 Filza 查看日志：
   ```bash
   cat /var/mobile/wxkbd_diag.log
   ```
   正常应看到：
   - `LOADED in com.tencent.wetype.keyboard ...` → 扩展进程已加载 dylib（若没有这行，说明扩展没注入）
   - 点语音时的 `[UIInputViewController] BLOCK wetype://...` → 拦截生效（不再跳转）

## 若仍跳转，把日志发我，按下面判断
- **没有 `LOADED in ...keyboard`**：扩展没注入 → 重做「同时注入 wxkb_plugin.appex」。
- **有 LOADED 但点语音没有 `BLOCK` 行**：跳转走的不是 openURL（可能是内部
  `handleVoiceRecordOpenURL:` 之类直接拉起，或别的机制）→ 把日志全文发我，我据此 hook 正确方法。
- **有 BLOCK 但仍跳**：拦截返回值被忽略 → 发我日志，我换更底层的拦截（如
  `-[UIInputViewController dismissKeyboard]` 后直接内部录音路径）。
